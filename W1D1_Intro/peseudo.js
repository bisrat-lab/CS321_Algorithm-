/*
Algorithm removeDups(L)
Input a list L
Output a list M contanining the distinct element of L
M <-- new List
for i <- 0 to L.size()-1 do
    if not M.contains(L(i)) then
        M.add(L[i])
        return M

*/
